*.DS_Store

# Nextflow runtime artifacts
work/
.nextflow/
.nextflow.log*
results/
*.dag.dot

# Raw screen recordings (source footage for assets/*.gif, *.png — not tracked)
*.mov

# Scratch/handoff material (source docs for features, not pipeline code)
tmp/
